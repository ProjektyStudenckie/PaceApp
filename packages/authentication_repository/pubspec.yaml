name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cache:
    path: ../cache
  equatable: ^2.0.0
  firebase_auth: ^1.0.0
  firebase_core: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter